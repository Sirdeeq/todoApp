import React from "react";

function Function(props){
    return(
           <h1>Not Loading  because we have finish Loading the API</h1>

    )
    // if (props.isLoading === true) {
    //     return(
    //       <h1>Loading...</h1>
    //     ) 
    //   }
    //   return(
    //       <h1>Not Loading  because we have finish Loading the API</h1>
    //     )
    //   }
  
    }    

export default Function;